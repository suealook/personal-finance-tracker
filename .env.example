# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Your numeric Telegram user ID from @userinfobot (whitelists the bot to only you)
TELEGRAM_ALLOWED_USER_ID=

# The ID from your Google Sheet's URL: https://docs.google.com/spreadsheets/d/<THIS_PART>/edit
GOOGLE_SHEET_ID=

# Path to the Google service account JSON key file
GOOGLE_SERVICE_ACCOUNT_FILE=data/credentials/service_account.json

# Anthropic API key from console.anthropic.com
ANTHROPIC_API_KEY=

# Model used for parsing + monthly analysis
CLAUDE_MODEL=claude-sonnet-5

# Flask local dashboard port
WEB_PORT=5000

# Optional locally (bound to 127.0.0.1, trusted). Required when deployed as the
# Home Assistant add-on, where the dashboard is LAN-reachable — set it there via
# the Configuration tab instead of this file.
DASHBOARD_PASSWORD=
